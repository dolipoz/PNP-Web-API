name: 'Lanzar Terraform'
on:
  workflow_dispatch:

jobs:
  terraform:
    name: 'Lanzar Terraform'
    runs-on: ubuntu-latest

    steps:
    # Importar Checkout para cargar el repositorio en el runner
    - name: Cargar Repositorio al Workflow
      uses: actions/checkout@v6
      with:
        # Para importar el repositorio se necesita generar un token en github
        token: ${{ secrets.TOKEN_GITHUB }}
    - name: Mover archivos terraform
      run: mv Terraform/* ./
    # ------------------------------- Importar python para instalar awscli -------------------------------
    - name: Importar python
      uses: actions/setup-python@v6
      with:
        python-version: '3.10'
    - name: Instalar awscli para subir ficheros al S3
      run: |
        python -m pip install --upgrade pip
        pip install awscli
    # Cargamos las credenciales de AWS para usar awscli y terraform
    - name: Configurar Credenciales AWS
      uses: aws-actions/configure-aws-credentials@v6
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ secrets.AWS_REGION }}

    # ------------------------------- Terraform -------------------------------
    - name: Cargar Terraform
      uses: hashicorp/setup-terraform@v4
    # Iniciar el módulo de terraform en el workdir por defecto
    - name: Iniciar Terraform
      run: terraform init
    # Ejecutamos terraform apply para crear la infrestructura
    - name: Aplicar Terraform
      run: terraform apply --auto-approve

    # Guardamos el .tfstate en un bucket creado personalmente en una cuenta AWS
    # En un secret se almacena el nombre del bucket por ejemplo: mi-cubo
    - name: Guardar .tfstate en el Bucket
      run: aws s3 cp terraform.tfstate s3://${{ secrets.AWS_S3 }}/Terraform/ --region ${{ secrets.AWS_REGION }}

    # Exportar las variables de terraform a variables del sistema
    - name: Exportar variables terraform
      run: |
        if ! command -v jq &> /dev/null; then
          sudo apt install jq -y
        fi
        TF_OUTPUT=$(terraform output -json)
        echo "$TF_OUTPUT" | jq -r 'to_entries[] | "export " + .key + "=" + (if .value.value|type == "array" then "\"" + (.value.value | join(",")) + "\"" else "\"" + (.value.value | tostring) + "\"" end)' > variables-terraform.sh
        chmod +x variables-terraform.sh

    # ------------------------------- ECR Docker -------------------------------
    # Creamos el repositorio dentro del ECR recién creado donde almacenaremos las imágenes creadas con docker build
    - name: Crear repositorio ECR de AWS para almacenar las imágenes creadas por docker
      run: |
        source variables-terraform.sh
        export NombreServidor=${{ secrets.HTTP_SERVER }}
        export AliasServidor=${{ secrets.HTTP_ALIAS }}
        export ImagenApache=${{ secrets.AWS_ACCESS_KEY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/apache/${{ secrets.ECR_Repo }}:latest
        export ImagenMysql=${{ secrets.AWS_ACCESS_KEY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/mysql/${{ secrets.ECR_Repo }}:latest
        envsubst < Docker/apache/https.conf | tee Docker/apache/https-var.conf

        aws ecr create-repository --repository-name ${{ secrets.ECR_Repo }} --region region

        aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCESS_KEY_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com
        docker build -t AWS/apache:latest ./Docker/apache
        docker build -t AWS/mysql:latest ./Docker/mysql
    # ------------------------------- EKS Kubernetes -------------------------------
    # Instalamos eksctl y kubectl
    - name: Instalar y configurar eksctl y kubectl
      run: |
        ARCH=amd64
        PLATFORM=$(uname -s)_$ARCH

        curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
        chmod +x kubectl
        mkdir -p $HOME/bin && cp kubectl $HOME/bin/kubectl && export PATH=$HOME/bin:$PATH

        curl -LO "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_$PLATFORM.tar.gz"
        tar -xzf eksctl_$PLATFORM.tar.gz
        mv eksctl /usr/local/bin

    # Conectamos kubectl con el cluster para crear el despliegue con el servicio web
    - name: Conectar kubectl con cluster y desplegar apache y mysql al EKS
      run: |
        source variables-terraform.sh

        aws eks update-kubeconfig --region ${{ secrets.AWS_REGION }} --name $nombre_cluster

        ### Creamos el secret de la contraseña de mysql
        kubectl create secret generic sql --from-literal=contrasena=${{ secrets.MYSQL_ROOT_PASSWORD }}
        
        ####### Añadimos los drivers de csi para volumenes en efs #######
        kubectl apply -k "github.com/kubernetes-sigs/aws-efs-csi-driver/deploy/kubernetes/overlays/stable/ecr/?ref=release-1.5"

        envsubst < kubernetes/volumenes/VP-HTML.yml | tee VP-HTML-Var.yml
        kubectl apply -f ./VP-HTML-Var.yml
        kubectl apply -f ./kubernetes/deployments/mysql.yml
        kubectl apply -f ./kubernetes/deployments/apache-php.yml
    # ------------------------------- Artefacto -------------------------------
    - name: Subir archivo
      uses: actions/upload-artifact@v4
      with:
        name: archivo
        path: archivo.txt
        retention-days: 1